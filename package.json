{
  "name": "food-delivery-app",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install -g vite && npm install --prefix frontend && npm install --prefix admin && npm install --prefix backend && echo 'Building frontend...' && cd frontend && vite build && echo 'Building admin...' && cd ../admin && vite build && echo 'Listing admin dist contents:' && ls -la dist/",
    "start": "cd backend && npm start"
  }
}
