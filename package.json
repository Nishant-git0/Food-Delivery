{
  "name": "food-delivery-app",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install -g vite && npm install --prefix backend && npm install --prefix frontend && npm install --prefix admin && cd frontend && vite build && cd ../admin && vite build",
    "start": "cd backend && npm start"
  }
}
